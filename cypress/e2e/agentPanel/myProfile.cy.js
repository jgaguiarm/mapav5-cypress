describe('template spec', () => {
  beforeEach(() => {
      cy.viewport(1900,1080)
      cy.loginWith(`${oppotunityAdminEmail}`,`${oppotunityAdminPassowrd}`)
  })

  it.skip("Valida que é possível alterar dados do Agente ", () => {
    
  })

  


})